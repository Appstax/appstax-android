#!/usr/bin/env bash -eu
# Create distributable jar

cd "$(dirname "$0")"/..

SOURCE="src/appstax/build/libs/appstax.jar"
TARGET="dist/appstax-android-1.0.0.jar"

cd src
./gradlew jarRelease
cd ..

rm -f "$TARGET"
cp "$SOURCE" "$TARGET"
